{
  "name": "@priorauth/passport-cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "priorauth": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "echo \"@priorauth/passport-cli: no package tests yet\""
  },
  "dependencies": {
    "@priorauth/passport-core": "workspace:*",
    "commander": "^14.0.2"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
